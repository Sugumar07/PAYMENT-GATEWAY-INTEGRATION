// Online C compiler to run C program online
#include <stdio.h>
#include<string.h>

int main()
    {
         int a[]={13,6,7,12};
         int i,j,b;
         for(i=0;i<sizeof(a);i++){
             for(j=i+1;j<sizeof(a);j++){
             if(a[i]>a[j]){
                 printf("%d",-1);
             }     
            else
            {
                b=a[j];
            }     
             }
         }
         return 0;
        }    
         
        
           